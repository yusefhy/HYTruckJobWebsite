Options -MultiViews
RewriteEngine On

# Serve existing files and directories directly (sitemap.xml, robots.txt, etc.)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# SPA fallback
RewriteRule ^ /index.html [L]
